---
    const { data, index } = Astro.props;
    const { snippet, statistics } = data;
---

<img alt={snippet.title} src={snippet.thumbnails.default.url} />
<p>{snippet.title}</p>
<p>{snippet.channelTitle}</p>
<p>{statistics.viewCount} Views</p>
