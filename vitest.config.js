/// <reference types="vitest" />
import { getViteConfig } from 'astro/config';

export default getViteConfig({
    test: {
        // empty, just adding config file becuase example app has it
    },
});
